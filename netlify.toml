[build]
  command = "cd xwiggy-app && npm install --legacy-peer-deps && npm run build -- --prod"
  publish = "xwiggy-app/dist/xwiggy-app"

[build.environment]
  NODE_VERSION = "16"
  NODE_OPTIONS = "--openssl-legacy-provider"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
